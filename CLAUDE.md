# Claude / multi-tool entry

Read **`AGENTS.md`**, then follow **`.cursor/rules/*.mdc`** (same rules Cursor auto-loads).

Confirm WoW API with MCP or the owning module. Put durable patterns in the architecture or lua-safety rule. Finish via the change checklist (lint; ask before version bump).
