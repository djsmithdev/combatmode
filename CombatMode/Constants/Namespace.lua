---------------------------------------------------------------------------------------
--  Constants/Namespace.lua — CONSTANTS — CM.Constants namespace initialization
---------------------------------------------------------------------------------------
--  Creates the empty CM.Constants table. Must load before every other Constants/*
--  file. No tables of its own — those live in the sibling Constants modules.
---------------------------------------------------------------------------------------
local _, CM = ...

CM.Constants = {}
